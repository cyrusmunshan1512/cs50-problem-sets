#include <stdio.h>
#include <cs50.h>
#include <string.h>

int main(void)
{
    string name = get_string("Whats your name? ");


    int length = strlen(name);
    // i need some way by which i would be able to calc the no of '0' in a name to subtract that from the final answer and display the number of letters without '0'.
    
}